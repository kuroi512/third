import 'package:flutter/material.dart';

class CategoryMealsScreen extends StatelessWidget {
//..

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Хоолны орц'),
      ),
      body: Center(
        child: Text(
          'Хоолны орцны хэсэг',
        ),
      ),
    );
  }
}
